import {useState} from 'react';import {resolveMediaUrl} from '../api/museumApi';export default function Gallery({items=[]}){const [m,setM]=useState(null);return <><div className='grid grid-cols-2 md:grid-cols-4 gap-3'>{items.map(i=><button key={i.id} onClick={()=>setM(i)} className='card p-2 text-left'><img src={resolveMediaUrl(i.image)} alt={i.imageAlt} className='w-full h-28 object-cover rounded'/><p className='text-sm mt-1'>{i.title}</p></button>)}</div>{m&&<div className='fixed inset-0 bg-black/70 flex items-center justify-center p-4' onClick={()=>setM(null)}><div className='bg-white p-4 rounded max-w-lg'><img src={resolveMediaUrl(m.image)} alt={m.imageAlt}/><h4>{m.title}</h4><p>{m.description}</p></div></div>}</>}
