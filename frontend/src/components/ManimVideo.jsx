import {useState} from 'react';import {resolveMediaUrl} from '../api/museumApi';export default function ManimVideo({settings}){const [ok,setOk]=useState(true);return <div className='card p-4'><h3>{settings.videoTitle}</h3><p>{settings.videoDescription}</p>{ok?<video controls className='w-full mt-2' onError={()=>setOk(false)}><source src={resolveMediaUrl(settings.videoUrl)} type='video/mp4'/></video>:<p className='mt-3'>{settings.videoPlaceholderMessage}</p>}</div>}
