import {resolveMediaUrl} from '../api/museumApi';export default function Timeline({items=[]}){return <div className='grid md:grid-cols-2 gap-4'>{items.map(i=><article key={i.id} className='card p-4'><img src={resolveMediaUrl(i.image)} alt={i.imageAlt} className='w-full h-56 object-cover rounded'/><h3 className='font-semibold mt-2'>{i.date} · {i.title}</h3><p>{i.description}</p></article>)}</div>}
